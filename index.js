function addingEventListener() {
    const input = document.getElementById('input');

function clickAlert() {
  alert('I was clickkkked!');
}

input.addEventListener('click', clickAlert);
}
addingEventListener();
